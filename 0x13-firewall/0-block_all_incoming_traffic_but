<<<<<<< HEAD
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp
=======
sudo ufw status

# if ufw isnot active, we can enable it by running:
sudo ufw enable

# set the default incoming policy to "deny" to block all incoming traffic by running
sudo ufw default deny incoming

#set the default ouytgoing policy to "allow" all outgoing traffic by running
#sudo ufw default allow outgoing

# Allow incoming traffic on TCP port 22, 443 and 80 by running:

sudo ufw allow 22/tcp
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp

# verify that the rules have been added by running:

sudo ufw show added

# Finally, enable ufw by running:
>>>>>>> a7531cbea9de3ab516756caea7fb6e4cc650c6e9
sudo ufw enable
